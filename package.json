{
  "name": "api",
  "version": "1.0.0",
  "description": "Backend API",
  "main": "server.js",
  "scripts": {
    "test": "test",
    "migrate": "node-pg-migrate",
    "start": "node server.js",
    "main": "node server.js",
    "heroku-postbuild": "yarn install && cd client && yarn install --only-dev && yarn install && yarn run build"
  },
  "engines": {
    "node": ">=12.0.0",
    "yarn": ">=1.22.4"
  },
  "author": "Jonah Lipsky",
  "license": "ISC",
  "dependencies": {
    "d3": "^5.16.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "node-pg-migrate": "^5.1.1",
    "pg": "^8.2.1"
  }
}
